# Run program
```shell
cd src
```
```shell
javac *.java
```
```shell
java Server
```
> **Run another terminal**

```shell
java Client
```

---
Reuse the lab2 `DataBase` , `Course` ,`Student`, `Overbook` Object and `ClientInput` format.
